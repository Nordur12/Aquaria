import React from 'react';
import { SafeAreaView, Text, StyleSheet, View, Image, TouchableOpacity, Alert, ScrollView, TextInput } from 'react-native';

export default function History(){

    return (
        <SafeAreaView>
          <Text>Hello History</Text>
        </SafeAreaView>
      );
}